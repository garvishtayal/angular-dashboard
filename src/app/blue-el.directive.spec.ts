import { BlueELDirective } from './blue-el.directive';

describe('BlueELDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueELDirective();
    expect(directive).toBeTruthy();
  });
});
